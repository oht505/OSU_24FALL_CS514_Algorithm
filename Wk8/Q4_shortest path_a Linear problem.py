# Suppose you want to find the shortest path from node s to t in a directed graph where edge (u,v) has length l[u,v] > 0.
# Write the shortest path problem as a linear program.
# Show that the dual of the program can be written as Max X[s]-X[t], where X[u]-X[v] <= l[u,v] for all (u,v) in E.